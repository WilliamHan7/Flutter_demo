import 'package:flutter/material.dart';

class CloudVillage extends StatelessWidget {
  const CloudVillage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('云村'),
    );
  }
}